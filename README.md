# mcp-client

# 1. from your home or project dir (Create & activate a venv)
    python3 -m venv venv
    source venv/bin/activate
    
# 2. Install required Python deps
    pip install --upgrade pip
    pip install -r requirements.txt

# 3. OpenAPI Secret key